/** Defaults for `~/.ai-pet/.env` (keys: `UserEnv` in `src/config/user-env.ts`). */

export const DEFAULT_ANIMATION_TICK_MS = 250;
export const DEFAULT_THEME_SETTING = 'auto';
export const DEFAULT_PET_SCALE = 1;
export const DEFAULT_MIN_PET_SCALE = 0.5;
export const DEFAULT_MAX_PET_SCALE = 2;
